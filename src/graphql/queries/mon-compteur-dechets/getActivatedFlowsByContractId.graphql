query getActivatedFlowsByContractId($filters: FlowFiltersInput) {
  flows(filters: $filters) {
    data {
      id
      attributes {
        name
        isActivated
        code
        createdAt
      }
    }
  }
}
