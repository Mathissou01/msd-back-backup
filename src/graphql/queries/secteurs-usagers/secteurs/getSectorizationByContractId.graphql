query getSectorizationByContractId($sectorizationId: ID) {
  sectorization(id: $sectorizationId) {
    data {
      attributes {
        name
        description
        polygonCoordinates
      }
    }
  }
}
