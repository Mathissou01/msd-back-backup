query deleteBackOfficeUserByUuid($uuid: String) {
  deleteBackOfficeUser(uuid: $uuid) {
    id
    name
    surname
    phone
    email
    uuid
    firstName
    lastName
    phoneNumber
    role
  }
}
