query getTerritoriesByContractId($contractId: ID!) {
  territories(filters: { contract: { id: { eq: $contractId } } }) {
    data {
      id
      attributes {
        numberOfInhabitants
      }
    }
  }
}
