mutation deleteSectorization($deleteSectorizationId: ID!) {
  deleteSectorization(id: $deleteSectorizationId) {
    data {
      id
      attributes {
        createdAt
        description
        name
        polygonCoordinates
        updatedAt
      }
    }
  }
}
