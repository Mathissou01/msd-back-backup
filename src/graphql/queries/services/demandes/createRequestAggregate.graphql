mutation createRequestAggregate($data: RequestAggregateInput!) {
  createRequestAggregate(data: $data) {
    data {
      id
      attributes {
        name
        requestService {
          data {
            id
          }
        }
        createdAt
        updatedAt
      }
    }
  }
}
