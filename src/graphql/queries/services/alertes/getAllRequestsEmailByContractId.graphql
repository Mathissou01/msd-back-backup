query getAllRequestsEmailByContractId($contractId: ID!) {
  requests(
    filters: { requestService: { contract: { id: { eq: $contractId } } } }
  ) {
    data {
      id
      attributes {
        requestType {
          id
          title
          email
          isEmail
          isTSMS
        }
      }
    }
  }
}
