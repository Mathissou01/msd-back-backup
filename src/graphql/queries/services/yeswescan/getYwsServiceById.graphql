query getYwsServiceById($serviceId: ID!) {
  yesWeScanService(id: $serviceId) {
    data {
      id
      attributes {
        startDate
        endDate
        serviceName
        shortName
        yesWeScanForm {
          data {
            id
          }
        }
      }
    }
  }
}
