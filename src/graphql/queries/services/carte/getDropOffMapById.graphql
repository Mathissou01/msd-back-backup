query getDropOffMapById($dropOffMapId: ID) {
  dropOffMap(id: $dropOffMapId) {
    data {
      id
      attributes {
        name
        phoneNumber
        downloadableFile {
          id
          linkText
          file {
            data {
              id
              attributes {
                name
                alternativeText
                hash
                mime
                size
                url
                provider
              }
            }
          }
        }
        mustKnow
        gpsCoordinates
        dropOffMapService {
          data {
            id
          }
        }
      }
    }
  }
}
