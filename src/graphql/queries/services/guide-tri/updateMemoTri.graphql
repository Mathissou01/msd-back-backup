mutation updateMemoTri(
  $updateRecyclingGuideServiceId: ID!
  $data: RecyclingGuideServiceInput!
) {
  updateRecyclingGuideService(id: $updateRecyclingGuideServiceId, data: $data) {
    data {
      id
      attributes {
        name
        isActivated
        memoName
        memoDesc
        memoFile {
          data {
            id
          }
        }
      }
    }
  }
}
