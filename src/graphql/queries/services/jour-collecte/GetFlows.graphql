query getFlows($contractId: ID) {
  flows(
    filters: {
      contract: { id: { eq: $contractId } }
      isActivated: { eq: true }
    }
  ) {
    data {
      id
      attributes {
        name
      }
    }
  }
}
